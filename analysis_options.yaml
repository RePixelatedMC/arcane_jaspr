include: package:lints/recommended.yaml

analyzer:
  exclude:
    - arcane_jaspr_neon/**

linter:
  rules:
    - prefer_const_constructors
    - prefer_const_declarations
    - prefer_final_fields
    - prefer_final_locals
    - avoid_print
