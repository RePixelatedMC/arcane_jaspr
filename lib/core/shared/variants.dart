/// Color variants for semantic meaning.
enum ColorVariant {
  primary,
  secondary,
  destructive,
  success,
  warning,
  info;
}

/// Style variants for visual treatment.
enum StyleVariant {
  solid,
  outline,
  ghost,
  link;
}
